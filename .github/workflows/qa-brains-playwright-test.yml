name: QA Brains Playwright Test

on:
  schedule:
    - cron: '0 8 * * *'  # Every day at 4:00 PM PHT

  workflow_dispatch:
    inputs:
      commit_id:
        required: false
        description: The commit SHA triggering this workflow
      commit_message:
        required: false
        description: The commit message
  push:
    branches:
      - main
      - development
      - 'feature-*'
      - 'enhancement-*'

  pull_request:
    branches:
      - main
      - development
      - 'feature-*'
      - 'enhancement-*'

jobs:
  run-playwright:
    uses: ./.github/workflows/run-qa-brains-playwright-test.yml
    with:
      commit_message: ${{ github.event.head_commit.message || github.event.inputs.commit_message || '' }}
      event_type: ${{ github.event_name }}
    secrets: inherit
